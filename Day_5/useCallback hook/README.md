# useCallback Hook

This folder teaches caching a function reference so memoized children can avoid useless re-renders.

## What to learn

- Stable function identity
- Why it matters with React.memo

## Practice checklist

- Pass one memoized handler to a child