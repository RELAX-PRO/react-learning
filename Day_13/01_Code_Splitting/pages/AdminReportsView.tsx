import React from 'react';

/**
 * ============================================================================
 * MECHANICS: Standard React Component
 * ----------------------------------------------------------------------------
 * This is a basic functional component. In the context of our Code Splitting
 * example, this component will be bundled into its own JavaScript chunk and 
 * requested only when the user navigates to the /reports route.
 * ============================================================================
 */
const AdminReportsView = () => { // Defines the functional component
  return <div>Admin Reports Route</div>;
};

export default AdminReportsView;
